spring.datasource.url=${DB_URL}
spring.datasource.username=${DB_USER}
spring.datasource.password=${DB_PASSWORD}
spring.datasource.driver-class-name=${DB_DRIVER}
# Configuraci�n de Hibernate y JPA
# Define c�mo Hibernate gestiona la creaci�n y actualizaci�n del esquema de la base de datos
# "none" no actualiza la base de datos de forma automatica y se deja la gesti�n manual en schema.sql y data.sql
spring.jpa.hibernate.ddl-auto=none

# Muestra las consultas SQL generadas en la consola
spring.jpa.show-sql=true

# Formatea las consultas SQL para que sean m�s legibles en la consola
spring.jpa.properties.hibernate.format_sql=true

# Dialecto específico de Hibernate para MariaDB (es compatible con MySQL)
# spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect

# Inicialización de la base de datos
# Esta propiedad asegura que Spring Boot ejecuta los archivos la estrucutra (schema.sql) y de carga de datos (data.sql) al inicio
spring.sql.init.mode=always

# Configuración de los logs del datasource
# Muestra detalles adicionales sobre la conexi�n a la base de datos
logging.level.org.springframework.jdbc.datasource.DataSourceTransactionManager=ERROR

# N�mero máximo de conexiones que puede abrir el pool de conexiones
spring.datasource.hikari.maximum-pool-size=10

# Configuraci�n del tama�o m�ximo para la subida de archivos
spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB
# Ruta para guardar las im�genes, proveniente de una variable de entorno
upload.path=${UPLOAD_PATH}